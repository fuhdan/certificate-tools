import React from 'react';

const Header = () => (
  <div className="header">
    <h1>Certificate Handler</h1>
    <p>Verify and analyze X.509 certificates and CSRs</p>
  </div>
);

export default Header;